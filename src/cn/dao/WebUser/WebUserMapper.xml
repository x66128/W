<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.dao.WebUser.WebUserMapper">
	<select id="getWebUser" resultType="webUser" parameterType="String">
		select * from web_user where user=#{user}
	</select>
	
</mapper>